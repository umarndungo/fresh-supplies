# Frontend
NEXT_PUBLIC_APP_NAME=Fresh Supplies
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_DEV_AUTH_BYPASS=false

# Database (Docker Compose + db/bootstrap.sh). Copy this file to .env and edit
# these to whatever you like — they stay private in .env (gitignored). The values
# below are the dev defaults and must match the DATABASE_URL in backend/.env
# (password @ is URL-encoded as %40 there).
DB_NAME=freshroute
DB_USER=freshrouteadmin
DB_PASS=freshroute@2120